export enum Enviroments {
    production = "Production",
    local = "Local"
}

export enum Sortfields {
    Name = "name",
    Rating = "rating",
}

export enum OrderTypes {
    ASC = "asc",
    DESC = "desc"
}